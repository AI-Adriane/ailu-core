[package]
name = "ailu-agents-core"
description = "Agent patterns over the runtime — ReAct loop, tools, approval gating (Rust port of @ailu/agents-core)."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
ailu-graph-core = { path = "../graph-core" }
ailu-graph-runtime = { path = "../graph-runtime" }
ailu-llm-gateway = { path = "../llm-gateway" }
ailu-fs-backend = { path = "../fs-backend" }
ailu-memory = { path = "../memory" }
ailu-skills = { path = "../skills" }
async-trait = "0.1"
futures-util = { version = "0.3", default-features = false, features = ["alloc"] }
# ADR 0029: real nested/enum/format JSON-Schema conformance for structured output.
# default-features = false drops HTTP/file `$ref` resolution — we only validate
# self-contained schemas, so this stays lean and avoids `$ref`-driven SSRF.
jsonschema = { version = "0.46", default-features = false }
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = "0.10"
thiserror = { workspace = true }

[dev-dependencies]
ailu-artifact-store = { path = "../artifact-store" }
tokio = { version = "1", features = ["rt", "macros"] }
