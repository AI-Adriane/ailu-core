# Build the Docusaurus documentation site and deploy it to GitHub Pages.
# Serves at https://ai-adriane.github.io/ailu-core/ (baseUrl /ailu-core/).
# One-time setup: repo Settings -> Pages -> Source = "GitHub Actions".
name: Docs

on:
  push:
    branches: [main]
    paths:
      - "docs-site/**"
      # Pages include the tested example files, and the version comes from the SDK package.
      - "packages/graph-sdk/examples/**"
      - "packages/graph-sdk/package.json"
      - ".github/workflows/docs.yml"
  # On pull requests, build only: a broken link or a missing example file fails the check.
  pull_request:
    paths:
      - "docs-site/**"
      - "packages/graph-sdk/examples/**"
      - "packages/graph-sdk/package.json"
      - ".github/workflows/docs.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Allow one concurrent deployment; don't cancel an in-progress one.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: docs-site
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm ci
      - run: npm run build
      - if: github.event_name != 'pull_request'
        uses: actions/configure-pages@v6
        with:
          # Create + configure the Pages site (source: GitHub Actions) on first run,
          # so no manual Settings -> Pages toggle is needed.
          enablement: true
      - if: github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@v5
        with:
          path: docs-site/build

  deploy:
    needs: build
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
