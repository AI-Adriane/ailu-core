{
  "name": "@ailu/plugin-mcp",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "stdio MCP server that runs the Ailu engine (@ailu/graph-sdk) in-process — execute governed agents/graphs with the real suspend-on-sensitive-tool / resume-after-approval loop — plus validate_graph + compile_graph_yaml via @ailu/napi.",
  "license": "Apache-2.0",
  "main": "server.mts",
  "scripts": {
    "smoke": "node --import tsx smoke.mts"
  },
  "dependencies": {
    "@ailu/approval-engine": "workspace:*",
    "@ailu/graph-sdk": "workspace:*",
    "@ailu/napi": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "tsx": "^4.20.6"
  }
}
